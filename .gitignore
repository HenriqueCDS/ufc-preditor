# Python
__pycache__/
*.pyc
.venv/
venv/

# backend/ml/artifacts/ (model.joblib, fighters.csv, fights.csv, ...) IS
# committed on purpose: Vercel's build step can't run `python -m ml.train`
# (needs sklearn/xgboost just to train, takes ~35min). Regenerate locally
# and commit the diff whenever data/*.csv changes.

# Node / Next.js (web/)
web/node_modules/
web/.next/
web/out/
web/.env*.local

# Vercel
.vercel/

# Scraper browser profile (local Playwright session data)
scraper/.pw-profile/

# Editor / OS
.vscode/
.DS_Store
Thumbs.db
