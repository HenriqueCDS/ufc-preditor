# Runtime deps for the Vercel Python service (main.py).
# Vercel's Python runtime serves the ASGI `app` directly. For local dev,
# `pip install uvicorn` separately and run `uvicorn main:app`.
fastapi>=0.117

# ml/ package deps (training + inference).
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
joblib>=1.4

# xgboost-cpu, not xgboost: same `import xgboost`, but skips the ~300MB
# nvidia-nccl GPU dependency that pushed the bundle past Vercel's 500MB
# function limit. Only needed at inference because train.py picked it as
# the best model; drop it if a sklearn-only model wins after retraining.
xgboost-cpu>=2.0
